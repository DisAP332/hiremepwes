// Calendar automation is intentionally disabled for the first production MVP.
// The admin workflow is manual: review request, contact the client, and update status.
export async function createCalendarEventForBooking() {
  return null;
}
